# evaluating equality and boolean operator precedence

# Output:
# false
# true

# Program:

begin
  bool p = false ;
  bool q = true ;
  bool r = false ;
  println p == q && r ;
  println p == (q && r)
end
